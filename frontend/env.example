# API Base URL (backend API)
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Payee address for DvP transactions (optional, defaults to connected wallet)
# VITE_PAYEE_ADDRESS=0x...

# WalletConnect Project ID (REQUIRED for WalletConnect to work)
# Get your project ID from https://cloud.walletconnect.com
# WalletConnect wallets will NOT work without a valid project ID
# VITE_WALLETCONNECT_PROJECT_ID=your_project_id_here
# VITE_REOWN_PROJECT_ID=your_project_id_here (alternative name, same as above)
